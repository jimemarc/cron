[tox]
envlist = py34, coverage, doc

[testenv]
passenv = http_proxy https_proxy
deps =
    -rrequirements.dev.txt
changedir = {envtmpdir}
commands =
    {envpython} -c "import topology_common; print(topology_common.__file__)"
    flake8 {toxinidir}
    py.test \
        {posargs} \
        {toxinidir}/test \
        {envsitepackagesdir}/topology_common

[testenv:coverage]
basepython = python3.4
commands =
    py.test \
        --junitxml=../../../build/topology-test-results.xml \
        --cov=topology_common \
        --cov-report xml \
        --cov-report html \
        --cov-report term \
        {posargs} \
        {toxinidir}/test \
        {envsitepackagesdir}/topology_common

[testenv:doc]
basepython = python3.4
whitelist_externals =
    dot
commands =
    sphinx-build -q -b html -d doctrees {toxinidir}/doc/ html

[flake8]
exclude = .git,.tox,.cache,__pycache__,*.egg-info

[pytest]
addopts = --doctest-modules
